import React from "react";
import { createRoot } from "react-dom/client";
import AWTOApp from "./AWTOApp";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AWTOApp />
  </React.StrictMode>
);
